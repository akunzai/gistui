# GitHub Pages assets

Files here are published to [akunzai.github.io/gistui](https://akunzai.github.io/gistui/).

- `index.html` — landing page
- `demo.gif`, `gist-manager.png` — screenshots

User-facing reference docs (install, safety) live in [`reference/`](../reference/)
at the repo root — they are **not** deployed to Pages.

Local design notes may live in `docs/superpowers/` (gitignored).