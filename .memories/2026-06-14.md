# 2026-06-14

### [Candidate 14:59] gistui release needs a manual Homebrew tap bump (cross-repo)
- gistui is distributed via a separate personal tap `akunzai/homebrew-tap` (local `~/code/homebrew-tap`); `Formula/gistui.rb` is build-from-source (`cargo install`) and pins a release `url` + `sha256`, with `depends_on "gh"`.
- A gistui `vX.Y.Z` tag now auto-triggers `release.yml` (platform binaries) and `publish.yml` (crates.io publish, once the `CARGO_REGISTRY_TOKEN` repo secret is set). The tap is the **one manual cross-repo step**: bump `url` to the new tag and `sha256` from `curl -sL https://github.com/akunzai/gistui/archive/refs/tags/vX.Y.Z.tar.gz | shasum -a 256`.
- As of today: `Cargo.toml` is at 0.8.0. **crates.io is now published** (https://crates.io/crates/gistui) — packaging prep + `publish.yml` landed in PR #100; account + `CARGO_REGISTRY_TOKEN` are set. Post-publish docs (crates.io badge, `cargo install`/`cargo binstall` sections, `[package.metadata.binstall]`, landing-page hint) are in PR #102 (open) — completes the #100 checklist. Distribution options tracked in gistui issue #93.
- binstall caveat: `[package.metadata.binstall]` only takes effect from the **next** published version (v0.8.1+), since binstall reads metadata from the published crate. Verify with `cargo binstall --dry-run gistui` after the next release.
- Homebrew 6.0.0+ tap trust: the fully-qualified `brew install akunzai/tap/gistui` trusts only this formula; the `brew tap` + short-name flow needs `brew trust`.
- Promotion target: gistui `AGENTS.md` (Versioning/Conventions). Mirrors the Claude-only note at `~/.claude/projects/-Users-akunzai-code-gistui/memory/gistui-homebrew-tap.md` (consolidate on promotion).
