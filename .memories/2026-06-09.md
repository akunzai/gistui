### [Handoff 00:24] gistui MVP implementation
- **Goal**: Continue implementing the `gistui` Rust TUI MVP from `docs/superpowers/plans/2026-06-08-gist-tui.md`. User chose subagent-driven execution. Immediate next sub-goal is Task 4: local candidate discovery.
- **Progress**: Brainstorming/design/spec/plan completed and committed. Product/crate/binary/config name changed to `gistui`; local repo directory remains `/Users/akunzai/code/gist-tui`. Completed and reviewed tasks:
  - Task 1 scaffold: `f404755 chore: scaffold rust tui crate`
  - Task 2 domain/config/path normalization: `e81a92c feat: add config and domain models`
  - Task 3 ranking: `9152b2d feat: rank gist file matches`
  - Current branch: `main`; worktree was clean before handoff.
- **Verification**: Task 1 passed `cargo fmt --check`, `cargo check`; Task 2 passed `cargo fmt --check`, `cargo test config`, `cargo check`, `cargo clippy --all-targets -- -D warnings`; Task 3 passed `cargo fmt --check`, `cargo test ranking`, `cargo check`, `cargo clippy --all-targets -- -D warnings`. Task 3 also passed spec and code-quality subagent review after refining path matching to meaningful exact tokens.
- **Next Actions**: Resume `superpowers:subagent-driven-development`. Start Task 4 from the plan: replace `src/local.rs` with known config paths, local discovery, empty candidate mode, and tests. Require subagent to also run `cargo fmt --check`, `cargo check`, and `cargo clippy --all-targets -- -D warnings`. Continue per-task spec review, then code-quality review, then update checklist.
- **Blockers & Assumptions**: No active blocker. User installed Homebrew Rust; plain `cargo` is available. Do not revert existing commits. Keep product name `gistui`; do not reintroduce `gist-tui` except in historical file names/repo path.
- **Suggested Skills**: `superpowers:subagent-driven-development`, then `superpowers:verification-before-completion` before final completion. Use `receiving-code-review` when subagent reviewers find issues.

### [Handoff:done 01:00] gistui MVP implementation
- All 10 plan tasks complete on `main`. Final HEAD `da18734`. Tasks 4-10 done this session via subagent-driven-development (implementer + spec review + code-quality review per task). Commits: `203adcb` local discovery (T4), `074db6c` gh parsing (T5), `1ea98cb` diff+guards (T6), `792e654` tui state (T7), `d928049` execute actions (T8), `da18734` startup wiring (T9). T10 verification: 30 tests pass, `cargo fmt --check`/`cargo check`/`cargo clippy --all-targets -- -D warnings` clean, `cargo run -- --check` prints "gh is installed and authenticated" exit 0. No T10 fix commit needed.
- **Review-driven fixes applied (beyond verbatim plan, justified by Correctness > Conformance):** T6 added `confirm_action(None, true)` safety-gate test; T7 reset `gist_index` to 0 on local-selection change (latent bug before ListState); T9 split `run()`→`run_loop()` so terminal teardown ALWAYS runs (Critical: `load_startup_state()?` is the first fallible call after `enable_raw_mode`), plus graceful gh-list degradation (launch with empty gist list on fetch/parse error).

### [Candidate 01:00] gistui MVP is browse/rank only — action keys unwired by plan design
- Final holistic review finding: `tui::handle_key` handles only `q`/`Tab`/`Up`/`Down`. The Commands footer advertises `Enter diff  u upload  d download  n create  p pin` and README lists upload/download/create/pin safety rules, but those helpers (`actions::{upload_command,create_command,execute_command,execute_download,pin_mapping}`, `diff::{unified_diff,sha256_hex}`, `gh::fetch_gist_file_content`, state fields `diff_previewed`/`pending_action`) have NO live (non-test) callers. This is the plan stopping at Task 10 by design, not a defect. **Open product decision for next session:** either (a) wire action keys into `handle_key` (new feature work, needs a plan), or (b) adjust footer/README to stop promising unreachable actions. Not unilaterally changed since the plan specified that footer/README verbatim.
- **Deferred minor notes (non-blocking):** T4 missing pinned-path files silently skipped by `is_file()` filter; T5 single-gist fixture is thin; T8 `execute_download` has a theoretical TOCTOU + stderr-on-success discarded. All acceptable for MVP per reviewers.

### [Candidate 01:30] gistui next: wire read-class action keys (Enter diff + d download) — design approved
- User chose to wire action keys (new feature beyond original plan). Brainstormed + design approved for SCOPE = Enter (preview diff) + d (download). upload/create/pin = next round.
- Design: add `Screen` enum (List/Diff/ConfirmOverwrite) to `AppState`; `handle_key` returns a `KeyOutcome` intent (None/Quit/PreviewDiff/Download) so IO stays in `run_loop` and state transitions stay pure & unit-testable. Full-screen scrollable diff overlay (Enter opens, Esc back); inline `y/n` overwrite prompt; `d` only exists in Diff view → enforces `diff_previewed` safety gate naturally; download writes the Enter-fetched `preview_remote` (what-you-see-is-what-you-write, no re-fetch at d); errors captured into a `status` bottom-bar message (not `?`-propagated) so TUI never dies on a failed action. Reuses existing `gh::fetch_gist_file_content`, `diff::unified_diff`, `actions::{confirm_action,execute_download}` — mainly `src/tui.rs` changes + README wording.
- **MUST RE-ADD NEXT ROUND:** `pending_action` field is being REMOVED from `AppState` this round (download path uses `preview_*` fields directly, so `pending_action` would be unused/clippy-flagged). When wiring upload/create/pin next round, re-add `pending_action` (or equivalent) as needed.

### [Handoff 01:45] gistui overnight autonomous run — execute read-actions plan + repo setup + branch restructure
- **User went to sleep; will continue TOMORROW on a DIFFERENT laptop.** This is why mem-sync push at the end is critical. Authorized me to run the whole sequence autonomously.
- **Ordered tasks:** (1) Execute `docs/superpowers/plans/2026-06-09-gistui-read-actions.md` via subagent-driven-development (5 tasks: screen state machine+tests → run_loop IO dispatch → render branching → README → verify). (2) Run `tidy-commits` (NOTE: interactive rebase `-i` is BLOCKED in this env; commits are already clean one-per-task, so expect minimal/no action — do NOT do risky history rewrites). (3) `init AGENTS.md` (use agents-md skill). (4) Add GitHub CI workflow + Dependabot config. (5) mem-sync, then git restructure + push.
- **Git restructure (final step) — exact intent:** Remote `origin` = git@github.com:akunzai/gistui.git; remote `main` does NOT exist yet. Push ONLY the scaffold to remote `main` (scaffold = `f404755`). Put ALL implementation + docs + AGENTS.md + CI on a NEW feature branch (e.g. `feat/gistui-mvp`) and push it, so a PR can be opened. Plan: after all work commits on local main, `git switch -c feat/gistui-mvp` (captures everything), then `git branch -f main f404755` (NON-destructive — commits preserved on feature branch; do NOT use `reset --hard`), then `git push -u origin main` (scaffold only) + `git push -u origin feat/gistui-mvp`. DO NOT create the PR (memory rule: PR only on explicit request — "以利發PR" = enable, not create). Do NOT push local main beyond scaffold.
- **Constraints honored:** no `reset --hard`/`clean`; no PR creation; main on remote stays at scaffold only.

### [Candidate 02:30] read-actions plan COMPLETE (Enter diff + d download wired)
- All 5 tasks done on `main` via subagent-driven (impl + spec + quality review each). Feature commits: `d4205b2` screen state machine (T1, +3 review-fix tests: q-in-list/confirm quit, confirm-esc, u16 scroll-cap clamp, back_to_list clears preview_local), `8b1d7fb` run_loop IO dispatch (T2), `fa98596` render branching (T3), `5ed5df2` README (T4). Final verify: 45 tests pass, fmt/check/clippy(-D warnings) clean, `cargo run -- --check` exit 0.
- **DEVIATION from user-approved mockup (flag for user):** code review found the diff direction was backwards for a DOWNLOAD preview. Changed `unified_diff("gist", remote, "local", local)` → `unified_diff("local", local, "gist", remote)` so `+` = content arriving from remote, `-` = local content being removed (intuitive "how will my local file change?"). The mockup the user approved showed `--- gist / +++ local`; I judged the mockup was about LAYOUT not diff direction. **If user prefers the original direction, flip the args back in `tui.rs::preview_diff`.**
- **NOT verified:** interactive TUI smoke test — this env is headless (no TTY). Binary compiles/links/launches; user must verify Enter/diff-scroll/d/y-n/Esc/q interactively on their laptop tomorrow.
- frame.size() is correct for pinned ratatui 0.26.3 (NOT deprecated until 0.28); do not change to area().

### [Handoff:done 02:50] gistui overnight autonomous run COMPLETE
- **Resuming TOMORROW on a DIFFERENT laptop — pull this `.memories` log first (mem-sync pull).** Repo: /Users/akunzai/code/gistui.
- **Done:** (1) read-actions plan fully implemented + reviewed (see [Candidate 02:30]). (2) tidy-commits: history was ALREADY clean (14 cohesive conventional commits, no fixups) → no rewrite; also interactive rebase is blocked in this env. (3) AGENTS.md created + `CLAUDE.md` symlink → AGENTS.md (commit `f10469e`). (4) GitHub Actions CI (`.github/workflows/ci.yml`: fmt/clippy-Dwarnings/test/build on push-to-main+PR) + Dependabot (`.github/dependabot.yml`: cargo+github-actions weekly) (commit `852cb19`).
- **Git end-state (as instructed):** remote `origin` (github.com:akunzai/gistui) was empty. Pushed scaffold-only to `origin/main` (`f404755`, includes its 2 pre-scaffold design/plan doc ancestors — unavoidable, they predate scaffold). ALL impl+docs+AGENTS+CI on branch `feat/gistui-mvp` (tip `852cb19`, 16 commits after scaffold), pushed to `origin/feat/gistui-mvp`. Restructure was NON-destructive (`git branch -f main f404755` from the feat branch; no reset --hard). Local checkout left ON `feat/gistui-mvp`.
- **PR NOT created** (per rule). Open it at: https://github.com/akunzai/gistui/pull/new/feat/gistui-mvp (feat/gistui-mvp → main).
- **For user to verify/decide tomorrow:** (a) interactive TUI smoke test (Enter/diff-scroll/d/y-n/Esc/q) — never run, env headless. (b) diff-direction deviation from approved mockup (see [Candidate 02:30] — flip args in tui::preview_diff if you disagree). (c) review the big PR.
- **Next feature round:** wire `u` upload / `n` create / `p` pin; re-add `pending_action` to AppState (removed this round). Plan/spec under docs/superpowers/.

### [Handoff 11:39] gistui MVP polish session (2nd laptop) + pending history rewrite
- **Context:** Resumed on the other laptop, installed binary via symlink `~/.local/bin/gistui -> target/debug/gistui` (debug for iterate-and-test; release also works). Interactive TUI tested live by user this session.
- **Root-cause fixes shipped this session (systematic-debugging):**
  - **Right pane was blank** — `gh gist list --json` does not exist in gh 2.93 (`--json` unsupported on `gist list`), so `fetch_gist_list_json` always failed and silently degraded to empty. Fixed: fetch via `gh api /gists?per_page=100`; `GhGist` now maps REST shape (`files` as object map → `BTreeMap` for deterministic order, `updated_at` snake_case, `description: Option<String>` for null). Fixture `tests/fixtures/gh/gist-list.json` updated to REST shape + null-description case.
  - **Keys "did nothing"** — list render used plain `List` with no selection/focus indicator; state changed invisibly. Fixed: `render_stateful_widget` + `ListState`, `▶ ` highlight, focused pane = Cyan border + `[focus]` title + reversed highlight.
- **Feature/UX changes this session (all user-approved):**
  - Local discovery scoped to **cwd only** (removed hardcoded known-config-paths + pinned injection; pinned still marks cwd files). Left pane shows **filename only**; cwd shown in `Local · <cwd>` title.
  - Download model: writes to **`cwd/<gist-filename>`**. `d` in list = direct download when target absent; if a same-named target exists, show its diff then `y/n` overwrite confirm. `Enter` = explicit preview-only diff. No-local-selected → right pane lists ALL gists unranked so you can still download into an empty dir. Startup focus = Gist pane.
  - List UX: `t` toggles gist rows description↔id view; `Left/Right` horizontal scroll (per-pane, cap = longest row); match strength shown as **stars** (⭐⭐⭐ ≥1000 exact/pinned, ⭐⭐ ≥250 path-hint, score 1/0 = none) instead of numeric `(score)`. Diff overlay also scrolls `Left/Right` (`Paragraph::scroll` x-offset). After download, left list **refreshes** (`refresh_locals`) and the `Downloaded …` status clears on any key.
- **Verification:** 61 unit tests pass; `cargo fmt --check` / `cargo clippy --all-targets -D warnings` / build all clean. Interactive scroll/keys confirmed by user. AGENTS.md write-gate rule updated to match cwd-download + overwrite-confirm semantics.
- **BACKLOG (moved here out of README per user request — do NOT put these back in README):**
  1. Wire `u` upload / `n` create / `p` pin actions to keys (re-add `pending_action`).
  2. Gist list **filter** and **sort** controls.
  3. Gist **type** toggle: public / secret / all.
  4. **Loading screen** while gists fetch (startup blocks on `gh api` before first frame — make async/threaded).
  5. Gist **content preview** in the list (description already shown via `t`).
### [Handoff:done 12:10] history rewrite COMPLETE (main+feat rewritten & force-pushed)
- **Done exactly as approved.** Method (since `git filter-repo` absent and `rebase -i` BLOCKED): (1) backed up to `backup/main-20260609-115259` + `backup/feat-20260609-115259`; held session changes in tag `tidy-wip-holder` (`be81155`). (2) Built standalone **`superpowers-docs`** branch via `commit-tree` (only `docs/superpowers/`, the 4 spec/plan files), pushed to origin — superpowers docs now live ONLY there, not on main/feat. (3) **Folded** session work into old commits by cherry-pick replay onto `f404755`, overwriting each session file at its last-touching commit (gh.rs+fixture→parse commit; tui.rs+local.rs co-folded at the render commit so the `discover_local_candidates` signature change lands with its caller and every commit still compiles; README/AGENTS into their doc commits). Skipped the 2 read-actions docs-only commits. (4) `git filter-branch --prune-empty --tree-filter` over `main`+replay branch: removed `docs/superpowers/`, perl-renamed `gist[-_]tui`→`gistui` in Cargo.toml/Cargo.lock/src/main.rs, pruned the now-empty docs-only ancestors.
- **Result pushed (force-with-lease):** `main` = single commit `80dafc4 chore: scaffold rust tui crate` (gistui, no superpowers). `feat/gistui-mvp` = `d09c272`, 14 commits on main (session work folded in, no superpowers, all gistui). `superpowers-docs` pushed new. Verified: tip tree == intended final state (empty diff vs WIP excl. superpowers); `cargo fmt --check`/61 tests/clippy `-D warnings`/build all clean; scaffold (main) builds standalone for CI.
- **Local backups kept (NOT deleted — need user OK to remove):** branches `backup/main-…`/`backup/feat-…`, tag `tidy-wip-holder`. README no longer has a Planned section (backlog lives in [Handoff 11:39] above). **No PR created** (per rule) — open feat/gistui-mvp → main if desired.

### [Candidate 12:25] git-filter-repo now installed (use instead of filter-branch)
- User installed `git-filter-repo` via Homebrew on this laptop. For any future history rewrite (dir removal, rename across history, prune), prefer `git filter-repo` over `git filter-branch` — faster, safer, fewer foot-guns. (This session's superpowers-docs removal used filter-branch only because filter-repo was not yet installed.)

### [Candidate 13:30] backlog progress — type-filter, sort, write-actions DONE
- On `feat/gistui-mvp` after the history rewrite, implemented (each its own commit, gate-green):
  - `2c05b92` feat: filter gist list by visibility (`v` cycles all/public/secret, shown in Gists title)
  - `092a85f` feat: sort gist list by match/name/recency (`s` cycles, shown in title)
  - Write-actions (`u`/`n`/`p`) via full brainstorm→spec→plan→subagent-driven execution (7 commits `51885f9`..`cffcef3`): `upload_add_command`+`unpin_mapping` planners; generalized confirm screen via re-added `pending_action` (`ConfirmOverwrite`→`Confirm`); `p` pin/unpin toggle; `u` upload (B add direct / C diff+y/n overwrite, target = local filename into selected gist's id); `n` create (s secret / p public prompt); after upload/create re-fetch gist list; docs. 78 unit tests pass.
- **Spec + plan committed to `superpowers-docs` branch (local only, worktree /tmp/gistui-sp, NOT pushed):** `2026-06-09-gistui-write-actions-design.md`, `2026-06-09-gistui-write-actions.md`.
- **Decisions locked:** content-preview = lazy-load on a keypress (not yet implemented). Use `git filter-repo` (now installed) for any future history rewrite.
- **Backlog remaining (do NOT put in README):** (1) free-text filter/search by filename+description; (2) gist content preview (lazy-load on keypress); (3) loading screen (async gh fetch off the main thread). Then open PR feat/gistui-mvp → main.
- **Not yet pushed:** the new feat commits (type-filter, sort, write-actions) are local on `feat/gistui-mvp`; origin still at the post-rewrite `d09c272`.

### [Candidate 13:45] new backlog item — cache the gist list on disk
- User request: cache the first-loaded gist list to a local file so the next startup shows the last-known gists immediately (then refresh in the background), speeding up init. Complements the loading-screen work (which shows the TUI instantly + "Loading…" while fetching). NOT yet implemented — backlog. Do NOT put in README (backlog lives here).

### [Candidate 14:15] remaining 3 backlog items DONE (filter/loading/preview)
- `48b1fb7` feat: free-text filter `/` (live, filename+description, case-insensitive; combines with v/s; Enter apply, Esc clear)
- `9c7be3a` feat: loading — gist list fetched on background thread + mpsc, poll-based event loop (event::poll 150ms + try_recv), "Loading gists…" placeholder; blocking gh fetches (diff/download/upload/preview) draw a "Loading…" frame first via fetch_then()
- `8fd17b5` feat: content preview `Space` — scrollable read-only overlay of raw gist content, lazy-fetch + per-(gist_id,filename) cache (Screen::Preview, reuses diff_scroll/hscroll)
- 86 unit tests pass; fmt/clippy -D warnings/build all green.
- **All original backlog now implemented.** Remaining/new backlog (NOT in README): (1) gist-list disk cache for fast init [Candidate 13:45]; (2) optional: scroll long diff inside the Confirm screen (download/upload confirm currently can't scroll — consistent but minor).
- **Push status:** many feat commits local on `feat/gistui-mvp`, origin still at `d09c272`. User wanted PR after backlog — now due. Spec/plan for write-actions still local on `superpowers-docs` (worktree /tmp/gistui-sp, unpushed).

### [Handoff 14:40] ALL gistui backlog implemented — ready to push + PR
- Final two backlog items done on `feat/gistui-mvp`:
  - `17dfe73` feat: gist-list disk cache ($XDG_CACHE_HOME/gistui/gists.json) — new `src/cache.rs` (load/save best-effort, tempdir round-trip tests), `GistFile` gained serde derive, startup loads cache for instant list then bg fetch refreshes + re-caches.
  - `8378ac7` feat: scroll diff inside Confirm screen (↑↓←→ in download/upload overwrite confirm).
- **State: ALL backlog from this session complete.** 90 unit tests pass; fmt/clippy -D warnings/build green. debug binary at ~/.local/bin/gistui (symlink → target/debug).
- **Full feature set added this session (14 commits on feat, origin still at `d09c272`, UNPUSHED):** v visibility filter, s sort, write-actions (u upload / n create / p pin + upload_add/unpin planners + pending_action confirm refactor), / text filter, loading (bg thread + poll loop + Loading frames), Space content preview (lazy+cache), disk cache, confirm-screen scroll.
- **superpowers-docs branch:** write-actions spec+plan committed locally (worktree /tmp/gistui-sp), UNPUSHED.
- **NEXT (awaiting user):** (1) push feat/gistui-mvp (fast-forward, no force); (2) push superpowers-docs; (3) open PR feat → main (needs explicit user OK per rule). No further backlog items open.
- Reminders: u/n hit the real GitHub API. `git filter-repo` is installed for future rewrites.

### [Candidate 15:30] UX polish round complete (post-backlog)
- On feat/gistui-mvp after the 3 backlog items: `919631a` Enter previews diff from local pane too (removed focus guard); `216b669` responsive single-line word-wrapped footer (commands_hint + wrap_line_count, dynamic footer height); `b49ef63` diff overlay coloured (green +, red -, bold headers; preview stays plain); `b193164` diff screen accepts u upload as well as d download (shared upload_intent method; upload_add now back_to_list); `618b801` pane polish (scrollbar on overflow, 1-col padding, dim unfocused pane fg DarkGray, solid cyan selection bar, empty-state placeholders incl. "no gists match the filter"); `df3d9a3` `?` help overlay (Screen::Help, all keybindings grouped).
- 95 unit tests pass; fmt/clippy -D warnings/build green. debug binary rebuilt.
- **21 commits now unpushed on feat/gistui-mvp** (origin still d09c272). All backlog + UX polish done. Awaiting user: push feat, push superpowers-docs (spec/plan local), open PR feat→main.
- ratatui 0.26 note: `Rect::inner` takes `&Margin` (not `Margin`); `Padding`/`Scrollbar`/`ScrollbarState`/`Wrap` from `ratatui::widgets`.

### [Handoff:done 16:00] gistui v0.1.0 MVP RELEASED
- PR #13 (feat/gistui-mvp → main) merged on GitHub (merge commit `03f25e1`); tag `v0.1.0` pushed; GitHub Release created. CI (Test & Lint, checkout bumped to v6) green.
- Repo reframed: "A terminal UI for managing GitHub Gists" (README/Cargo.toml description/AGENTS/repo description).
- Roadmap: 12 enhancement issues opened (#1 pagination, #2 edit gist content, #3 delete, #4 pin sync direction, #5 manage pins, #6 word-level diff, #7 configurable discovery, #8 keybind/theme config, #9 preview cache refresh, #10 release-binaries CI, #11 async per-action fetches, #12 mockable gh / integration tests).
- Commits kept as-is (rebase -i blocked here; 30+ conventional commits = the story). superpowers-docs branch (brainstorm specs/plans) judged low ongoing value and DELETED (local+remote+worktree).
- Known: low-severity transitive `lru` Stacked-Borrows Dependabot alert — cannot fix via cargo update (pinned ratatui 0.26 constrains lru to 0.12.5); accepted/deferred.
- Open: feat/gistui-mvp branch still exists (merged) — optional to delete. mem-sync NOT pushed this session (user stayed on one laptop).

### [Candidate 16:30] post-release dependency maintenance done
- Dependabot opened 6 major-version cargo bumps after the v0.1.0 release. Handled on main:
  - Merged: crossterm 0.27→0.29 (#14), toml 0.8→1.1 (#15), and a consolidated PR #20 for similar 2→3 + dirs 5→6 + sha2 0.10→0.11. sha2 0.11 needed a code fix: `diff::sha256_hex` now hex-encodes the digest output by hand (`format!("{:x}", finalize())` no longer works under digest 0.11).
  - #18/#19 conflicted on Cargo.lock after #14/#15 merged → folded into #20; #16 needed the sha2 fix → also in #20. #16/#18/#19 closed as superseded.
  - **ratatui 0.30 (#17) deferred** → closed, tracked as issue #21 "Migrate to ratatui 0.30" (breaking: frame.size()→area(); ratatui stays pinned at 0.26).
- Direct push to main is blocked by the harness; dep work went via PR #20 (CI green, rebase-merged). main tip after: `e500f7a`. Local branches: main + memories only.
- 106 tests still pass; release build clean. Repo has no open PRs. Roadmap = issues #1–#12 + #21.

### [Candidate 10:00] issues #5/#6/#7/#9 implemented and merged
- All four via PR branches (each its own conventional commit, gate-green):
  - **#7 background scan + scan_depth** (`daa4854`): `spawn_local_scan` thread via mpsc; `r` toggles recursive; `scan_depth: u32` in `AppConfig` (default 2, serde-default); `run_loop` polls `local_rx` alongside `gist_rx`; local pane title shows `" …"` while scanning. `config.example.toml` updated.
  - **#9 cache invalidation** (`6ec2322`): `preview_gist_key: Option<(String,String)>` tracks current preview's cache key; `R` force-refresh evicts entry and re-fetches; upload paths (`upload_add`/`upload_replace`) evict cache on success.
  - **#5 pins view** (`8eefdbb`): `Screen::Pins`; `P` opens full-screen pin list; `x`/`d` unpins via `unpin_mapping_exact` (exact `(local_path, gist_id)` match, not greedy); `pins_index: usize` in `AppState`; footer `"↑↓ move · x unpin · Esc/q back"`.
  - **#6 inline diff** (`b3de4e7`): `similar::TextDiff::from_words`; contiguous del/ins runs paired N-to-N; changed words bold-red/bold-green, unchanged words plain-red/plain-green; unpaired lines fall back to plain; `apply_hscroll_spans` helper.
- README.md updated in the same PR #31 commit (user reminder: always update README with new features).
- Remaining open roadmap: #4 pin sync direction, #8 keybind/theme config, #11 async per-action fetches, #12 integration tests with mockable gh boundary.

### [Candidate 20:59] v0.2.0 RELEASED — mockable gh boundary + install script
- PR #36 (`feat/mockable-gh-boundary` → main) rebase-merged; tag `v0.2.0` pushed; Release workflow green; Release has all 10 assets (5 archives + 5 `.sha256`). main tip after merge `ca1cca9`.
- **#12 mockable gh boundary (closed):** new `actions::{CommandOutput, CommandRunner trait, SystemRunner, run_command}`. `gh` read fns now build pure `CommandPlan`s (`gist_list_plan`/`gist_view_plan`/`gh_version_plan`/`auth_status_plan`) + `*_with(runner)` variants + `check_gh_ready_with`; no-arg public fns kept as `SystemRunner` wrappers so tui.rs call sites are UNCHANGED. New `tests/gh_integration.rs`: `FakeRunner` implemented OUTSIDE the crate (proves the boundary is genuinely injectable), 9 tests, no network/no gh. Deliberate behavior change: exec path uses `from_utf8_lossy` not strict `from_utf8`.
- **install.sh (new):** `#!/usr/bin/env bash`, 3.2-safe; detects Linux/macOS + **Windows Git Bash/MSYS2** (MINGW*/MSYS*/CYGWIN*), downloads release asset, **SHA-256 verifies**, installs to `~/.local/bin`; `--version`/`--bin-dir`. `release.yml` now emits `.sha256` per archive (Unix `shasum`, Windows `Get-FileHash`) and uploads both. README has `curl … | bash` one-liner. Verified `bash -n`/`shellcheck`/`actionlint`.
- **Decided AGAINST (with user agreement):** auto-update (over-engineering for a gh-wrapper) and i18n (premature for a dev tool) — no issues opened.
- **New follow-up issues:** #32 polish TUI visuals → blocks #33 demo GIF; #34 crates.io publish (+ Homebrew tap); #35 CHANGELOG.md. User wants to defer the demo GIF until UI is polished.
- Open roadmap now: #4 pin sync direction, #8 keybind/theme, #11 async per-action, plus #32/#33/#34/#35.

### [Candidate 23:47] opened 5 feature issues (#37–#41) from user idea batch
- Assessed against code first; all 5 suitable, no overlap with existing open issues. gh natively supports needed flags (`gist edit -r/--remove`, `-d/--desc`; `gist create -d`) — verified via `--help`, so no REST PATCH needed.
- **#37** gist-level view (new `Screen`, group flat file rows by `gist_id`): view/edit description, delete whole gist; **change file-row delete to remove single file** (`gist edit -r`) instead of today's whole-gist `gist delete` (current bug: `X`/`delete_command` `actions.rs:151` nukes entire gist for any row). Biggest architectural change — model is file-centric (`GistFile` `domain.rs:26`), no gist entity.
- **#38** create with description (`create -d`); shares desc-input widget with #37.
- **#39** edit/redact content before upload (temp copy, never mutate local; keep overwrite invariant) + JSON pretty-print & recursive key-sort as a **pure unit-tested transform** (only `.json`).
- **#40** show version (`CARGO_PKG_VERSION`) + repo link in Help/About (pairs with #32).
- **#41** reverse ranking gist→local + local-pane sort modes mirroring `GistSort` (needs mtime from local scan).
- User explicitly added the JSON format/sort requirement to #39 mid-discussion.

### [Candidate] gh gist edit --desc opens an interactive editor (gotcha)
- `gh gist edit <id> --desc "x"` does NOT just set the description: with no `--add`/`--remove` it falls into gh's default content-edit flow and opens `$VISUAL`/`$EDITOR` on a temp file (confirmed in cli/cli edit.go editRun + observed live). Unusable inside the TUI.
- Fix for description-only update: `gh api --method PATCH /gists/<id> -f description=<desc>` (non-interactive; `-f` raw string so arbitrary text isn't type-coerced). Implemented in #37 `actions::edit_description_command`.
- `gh gist edit --add` / `--remove` are fine — both early-`return updateGist` before the editor flow, so they're non-interactive.

### [Candidate] #37 implemented → PR #43 (feat/gist-level-view, enhancement)
- New `Screen::Gists` (open `g`, lands on selected file's gist): inline description edit (`e`, REST PATCH not `gist edit --desc`), whole-gist delete (`X`), browser (`o`, moved from main list), sort `s` updated/created, filter `/` by desc/id (independent), visibility `v`, `←/→` hscroll. Main-list `X` now removes a single file (`gist edit --remove`) with only-file guard.
- `created_at` plumbed through GhGist/GistFile/GistGroup + fixture (44 literals patched). Blocking gh ops now show a centered "Working…" overlay (`render_loading_overlay`) — Confirm/Gists screens didn't surface `status` so they looked frozen; true spinner needs async (#11).
- Gate green, 141 tests. Architectural note: tui.rs has a SECOND `PendingAction` enum (its own, not `actions::PendingAction`) — the TUI uses the tui.rs one.

### [Candidate] #38 done → PR #44 (feat/create-with-description, enhancement)
- Create flow (`n`) now starts in the inline description editor (reuses #37 `editing_description`/`description_input`): type desc → Enter → s/p visibility. `create_command(local_path, public, description)` adds `--desc` when non-empty. 146 tests green.
- Remaining idea backlog: #39 (edit/redact before upload + JSON), #40 (version/link), #41 (reverse ranking + local sort). #37 merged; #43 (gist view) merged; #35/#42 (release-note categories) merged.

### [Candidate] #40 merged (#45), #41 → PR #46; #39 deferred to afternoon
- #40 (#45 MERGED): help screen About line `gistui v<ver> · <repo>` via CARGO_PKG_VERSION/REPOSITORY; added `repository` to Cargo.toml (helps #34).
- #41 (PR #46 open): focus-driven two-way ranking — unfocused pane ranks by focused selection; gist-focused now ranks LOCAL files by selected gist (`rank_local_files`). `s` cycles the FOCUSED pane's sort; local pane gains match/name/recent, mtime stored in new `LocalCandidate.modified` (pure compare). 154 tests.
- GOTCHA (durable): the two focus-driven rankings recurse if you evaluate the cross-pane selection eagerly (e.g. in a `match (self.focus, self.selected_x())` scrutinee) — it stack-overflowed once. Fix: only call the other pane's `selected_*()` INSIDE the matching focus branch. Regression test `ranking_helpers_terminate_in_either_focus`.
- NEXT (this afternoon): #39 edit/redact before upload + JSON pretty-print/key-sort.
- Docs debt: README/AGENTS were stale after #37 merged (X semantics, o moved, no gist-manager section). Caught up on the #44 branch (folded in to avoid same-section conflicts). Added AGENTS.md convention: user-facing key/screen/feature changes MUST update README + `?` help in the SAME PR (now auto-loaded project memory — apply going forward). User has flagged the docs miss twice.

### [Candidate 23:55] re-scoped #35 — no manual CHANGELOG.md
- Confirmed `release.yml:117` already has `generate_release_notes: true` (GitHub auto-notes, zero maintenance). gistui is a binary (not a depended-on crate) so a hand-written CHANGELOG.md only duplicates Release notes.
- Decided AGAINST: manual CHANGELOG.md, and release-drafter (overlaps with existing generate_release_notes — would need to reconcile two generators).
- New #35 scope: add `.github/release.yml` categorizing notes by PR label (Features/Bug Fixes/Documentation/Dependencies, exclude chore/ci), aligned with Conventional Commits; + document "every PR must carry a category label" in AGENTS.md (recommended) or a new CONTRIBUTING.md. Caveat: only covers PR-based merges, not direct-to-main commits; optional later autolabeler.
